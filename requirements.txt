docker>=7.0.0
requests>=2.31.0
PyYAML>=6.0.1
# systemd-python is an optional dependency that enables sd_notify integration
# (READY=1, WATCHDOG=1 signals) and the WatchdogSec feature in the systemd unit.
# ADRG runs correctly without it — the daemon simply won't notify systemd of its
# ready state. Install it if you want full systemd service type=notify support:
#   sudo apt install libsystemd-dev && pip install systemd-python
